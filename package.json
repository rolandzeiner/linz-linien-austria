{
  "name": "linz-linien-austria-card",
  "private": true,
  "version": "0.0.0",
  "description": "Lovelace custom card for the Linz Linien Austria HA integration",
  "type": "module",
  "scripts": {
    "build": "rolldown -c",
    "dev": "rolldown -c -w",
    "build:push": "rolldown -c && ./scripts/dev-push.sh --quiet"
  },
  "devDependencies": {
    "rolldown": "1.2.7",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "lit": "^3.3.3"
  }
}
