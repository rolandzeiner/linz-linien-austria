# Source of truth for ruff, mypy, and coverage. Pytest config stays in pytest.ini.
# Pre-commit and CI may still pass equivalent CLI flags — they are now
# redundant with this file but harmless. Trim them as confidence grows.

[tool.ruff]
# Track the CI Python pin (see .github/workflows/validate.yml).
target-version = "py314"
line-length = 88

[tool.mypy]
strict = true
ignore_missing_imports = true
files = ["custom_components/linz_linien_austria"]

[tool.coverage.run]
source = ["custom_components/linz_linien_austria"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "\\.\\.\\.",
]
show_missing = true
skip_empty = true
