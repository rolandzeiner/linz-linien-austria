# PHACC governs the test stack. It pins Home Assistant core and the whole
# pytest toolchain with `==`, so anything it already pins is listed here
# only if we need a constraint PHACC does not impose.
pytest-homeassistant-custom-component>=0.13.363,<0.14
# No floors for pytest-asyncio / pytest-cov / pytest-timeout / syrupy.
# PHACC pins all four with `==` (1.4.0 / 7.1.0 / 2.4.0 / 6.0.0 as of
# 0.13.361), so a floor here can never bind: it either matches PHACC and
# does nothing, or contradicts it and makes the install unresolvable.
# Dependabot has never proposed bumping one of them for exactly that
# reason. pytest.ini's `timeout` setting and `--cov` still work — both
# plugins arrive transitively via PHACC.
mypy==2.3.1
ruff==0.16.5
# pycares cap not needed: HA 2026.5+ pins aiodns 4.x which speaks the
# pycares 5 API natively, so the resolver picks pycares 5 transitively
# without us having to cap it.
